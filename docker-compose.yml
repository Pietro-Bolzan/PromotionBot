services:
  db:
    image: postgres:16
    container_name: amazonbot-postgres
    environment:
      POSTGRES_DB: amazonbot
      POSTGRES_USER: amazonbot
      POSTGRES_PASSWORD: amazonbot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
